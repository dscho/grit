# t0020-crlf

## Claim
- Claimed `t0020-crlf`, the single-failure entry point into the CRLF/conversion lane.

## Findings
- Current failing subtest is `checkout with existing .gitattributes`.
- `checkout main~1 && checkout main` rebuilt the index from tree entries with zero stat fields.
- Checkout skipped unchanged paths such as `three`, and wrote CRLF-smudged paths such as `.file`; both ended up stat-dirty to `diff-files`.

## Work
- Updated branch checkout stat refresh to preserve old stat tuples for skipped unchanged entries.
- Updated freshly written checkout entries to adopt worktree metadata even when CRLF smudge means raw bytes do not hash directly to the index blob.

## Verification
- `cargo build --release -p grit-cli` passes with the existing warning backlog.
- `./scripts/run-tests.sh t0023-crlf-am.sh t0020-crlf.sh t0000-basic.sh t0081-find-pack.sh` passes: `t0023` 2/2, `t0020` 36/36, `t0000` 92/92, `t0081` 4/4.
