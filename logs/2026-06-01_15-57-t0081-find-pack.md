# t0081-find-pack

## Claim
- Claimed `t0081-find-pack` as a t0 quick win in the pack/test-tool lane.

## Dependency notes
- Remaining t0 failures group into repo/env/init, conversion/encoding, filesystem/path utils, object/index/pack, refs/reftable, and docs-help lanes.
- `t0081-find-pack` is isolated to `test-tool find-pack` output over pack indexes; it does not require changing pack storage or object resolution.

## Findings
- The failing subtest is `repack everything into a single packfile`.
- Object lookup finds the right pack for all requested objects.
- The failure is output formatting: this test expects `.git/objects/pack/pack-*.pack`, while `test-tool find-pack` currently prints canonical absolute paths.

## Work
- Format pack paths relative to the current directory when possible, falling back to the original path otherwise.

## Verification
- `cargo build --release -p grit-cli`: pass with existing warnings.
- `./scripts/run-tests.sh t0081-find-pack.sh`: pass, `4/4`.
