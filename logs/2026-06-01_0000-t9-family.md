# t9 family pass sweep

## Goal

Make the current in-scope `t9` family tests fully pass, one file at a time, always selecting the
non-green file with the largest current failure count in `data/test-files.csv`.

## Initial status

- Branch: `cursor/t9-family-tests-passing-44bf`.
- Highest failure count at start:
  - `t9040-hash-object-types`: 1/28, 27 failing.
  - `t9060-mktag-verify`: 1/28, 27 failing.
  - `t9300-branch-delete-force`: 4/25, 21 failing.
  - `t9600-switch-branch-create`: 20/40, 20 failing.
- First target selected by CSV-order tie-break: `t9040-hash-object-types.sh`.

## Notes

- `t9040-hash-object-types.sh` appears to match the TESTING.md cwd-leak pitfall: its setup helper
  runs `grit init repo && cd repo`, and the setup test calls the helper without a subshell. Later
  test bodies are already subshell-wrapped and start with `cd repo &&`, so they likely attempt to
  enter `repo/repo`.
- `t9060-mktag-verify.sh` has the same setup-helper shape and likely requires the same verification
  after `t9040` is green.


## 2026-06-01 — `t9040-hash-object-types.sh`

- Built `target/release/grit` with `cargo build --release -p grit-cli`; build passed with existing warnings.
- Reproduced `t9040-hash-object-types.sh` at 1/28 through `./scripts/run-tests.sh`.
- Confirmed the failure pattern matched the cwd-leak setup issue.
- Wrapped the setup helper invocation in a subshell so `cd repo` does not leak into later tests.
- Re-ran `./scripts/run-tests.sh t9040-hash-object-types.sh --verbose`: now 28/28.
