# t6101 rev-parse parents

Claimed `t6101-rev-parse-parents.sh` from `t6-plan.md` at 35/38 passing.

Initial focus:

- Run the focused harness to identify the three remaining rev-parse parent/ancestor failures.
- Compare local and upstream tests, then read `git rev-parse` documentation for parent and peeling
  operators.
- Search existing revision parser behavior before changing code.

Findings:

- Local and upstream `t6101-rev-parse-parents.sh` are identical.
- `rev-parse` already expanded `^@`, `^!`, and `^-N` through the shared rev-parse helper.
- `rev-list` had a local parent shorthand expander that only handled `^!`, so `merge^-`,
  `merge^-1`, and `merge^-2` were treated as literal revision names.

Changes:

- Replaced the local `rev-list` `^!`-only expansion with
  `grit_lib::rev_parse::expand_parent_shorthand_rev_parse_lines`, preserving invalid shorthand
  fallthrough for the existing error path.

Validation:

- `cargo build --release -p grit-cli` completed with the existing warning backlog.
- Focus harness: `./scripts/run-tests.sh t6101-rev-parse-parents.sh --verbose` passes 38/38.
- Regression harness: `./scripts/run-tests.sh t6101-rev-parse-parents.sh t6001-rev-list-graft.sh t6012-rev-list-simplify.sh t6016-rev-list-graph-simplify-history.sh t6111-rev-list-treesame.sh --verbose` passes 38/38, 14/14, 42/42, 12/12, and 65/65.
- `cargo fmt`, `cargo check -p grit-cli`, `cargo test -p grit-lib --lib`, and
  `cargo clippy --fix --allow-dirty` completed with the existing warning backlog; unrelated
  fmt/clippy auto-fixes were restored.
