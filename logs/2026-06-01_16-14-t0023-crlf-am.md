# t0023-crlf-am

## Claim
- Claimed `t0023-crlf-am` after fixing the adjacent `t0020-crlf` checkout/stat issue.

## Findings
- The failing `git am -3` test left `foo` staged with zeroed ctime/mtime/dev/ino/uid/gid fields.
- `git diff-files --name-status --exit-code` then reported `M foo` immediately after `am`, despite matching content.

## Work
- Updated `am` staging to run the same CRLF clean conversion path used by `git add`.
- Built staged entries from filesystem metadata with `entry_from_metadata` instead of hand-written zero stat tuples.

## Verification
- `cargo build --release -p grit-cli` passes with the existing warning backlog.
- `./scripts/run-tests.sh t0023-crlf-am.sh t0020-crlf.sh t0000-basic.sh t0081-find-pack.sh` passes.
