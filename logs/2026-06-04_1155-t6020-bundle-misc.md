# t6020 bundle misc

## Goal

Finish `t6020-bundle-misc.sh`, currently the largest remaining t6 row at 13/37.

## Notes

- Claimed after completing `t6135-pathspec-with-attrs.sh`.
- Test scope covers bundle create/list/verify/unbundle behavior, prerequisite handling,
  stdin/stdout bundle paths, revision exclusions, and bundle fetch/clone compatibility.
- First increment adds parsing for common bundle create flags (`--stdin`, `--ignore-missing`,
  quiet/progress toggles), expands `^!` revision tokens into a positive tip plus parent
  prerequisites, parses prerequisite/capability header lines, prints Git-style `bundle verify`
  information, supports `-` as bundle stdin/stdout, and makes `bundle unbundle` print bundle heads
  instead of updating refs directly. `./scripts/run-tests.sh t6020-bundle-misc.sh --quiet`
  improves the row from 13/37 to 16/37.
- Validation before commit: `cargo fmt`, `cargo check -p grit-cli`, `cargo clippy --fix
  --allow-dirty` (existing warning backlog and failed auto-fix attempts remain; restored the
  unrelated `filter_process.rs` auto-fix), `cargo test -p grit-lib --lib`, focused
  `t6020-bundle-misc.sh`, `cargo build --release -p grit-cli`, and `git diff --check`.
- Third increment filters advertised bundle refs under `--max-count` to refs whose objects are
  actually written, while preserving explicit annotated tag objects. Focused
  `./scripts/run-tests.sh t6020-bundle-misc.sh --quiet` improves from 20/37 to 21/37.
- Validation before commit: `cargo fmt`, `cargo check -p grit-cli`, `cargo clippy --fix
  --allow-dirty` (existing warning backlog and failed auto-fix attempts remain; restored the
  unrelated `filter_process.rs` auto-fix), `cargo test -p grit-lib --lib`, and
  `git diff --check`.
- Second increment makes `bundle verify` print the hash algorithm field from `GIT_DEFAULT_HASH`,
  matching the ported harness where the variable is empty for SHA-1 runs. Focused
  `./scripts/run-tests.sh t6020-bundle-misc.sh --quiet` improves from 16/37 to 20/37.
- Validation before commit: `cargo fmt`, `cargo check -p grit-cli`, `cargo clippy --fix
  --allow-dirty` (existing warning backlog and failed auto-fix attempts remain; restored the
  unrelated `filter_process.rs` auto-fix), `cargo test -p grit-lib --lib`, focused
  `t6020-bundle-misc.sh`, `cargo build --release -p grit-cli`, and `git diff --check`.
