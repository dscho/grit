# t2 family 100% pass log

## 2026-06-01 20:00 UTC — Claim and setup

- Claimed the t2-family sweep in `plan.md`.
- Starting from the highest-failure in-scope t2 row in `data/test-files.csv`:
  `t2050-checkout.sh` at 8/80 passing, 72 failing.
- Branch: `cursor/t2-family-test-passing-0141`.
- Planned loop: build release, run selected file, fix first/root failure, rerun until green,
  then choose the next highest-failure t2 row.

## 2026-06-01 20:05 UTC — `t2050-checkout.sh` green

- Baseline `./scripts/run-tests.sh t2050-checkout.sh --verbose`: 8/80.
- Direct verbose run stopped at subtest 3: `git checkout master` failed because setup's `git init repo`
  created `main`, but the synthetic test hard-codes `master` throughout.
- Confirmed with `GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME=master` that the entire file passes 80/80
  without Rust code changes.
- Added an explicit `GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME=master` export to the test fixture before
  sourcing `test-lib.sh`.
- Verification: `./scripts/run-tests.sh t2050-checkout.sh --verbose` now passes 80/80 and updates
  `data/test-files.csv`/dashboards.
