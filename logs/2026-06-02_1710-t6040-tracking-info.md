## t6040-tracking-info

Goal: make `tests/t6040-tracking-info.sh` fully pass as the next tracking/refs t6 item.

Initial CSV state: 44 tests, 23 passed, 21 failing.

Worktree: `/private/tmp/grit-t6-family` on branch `wf/t6-family`.

Notes:
- Starting after `t6300-for-each-ref.sh` and `t6200-fmt-merge-msg-extra.sh` are green.
- First harness run improved from the CSV baseline to 29/44.
- Push test 28 failed because thin push-pack generation passed remote-only object IDs to the local
  `pack-objects` exclusion set. Fixed `build_thin_push_pack` to keep only haves available in the
  local object database.
- After rebuild, harness reached 31/44. Test 29 exposed missing blank-line preservation in
  multi-branch `status.compareBranches` output. Fixed long status header rendering to preserve
  internal blank lines emitted by `format_tracking_info`.
- After rebuild, harness reached 42/44. The next setup failure was `git push origin HEAD:main`
  while detached; Grit rejected the one-level destination even though the remote already had a
  unique `refs/heads/main`. Fixed push destination resolution for detached/object sources to allow
  a unique existing remote destination.
- Final verification: `cargo check -p grit-cli` passed with the existing warning backlog, release
  build passed, and `./scripts/run-tests.sh t6040-tracking-info.sh --verbose` passed 44/44.
