# t0000-basic

## Claim
- Claimed `t0000-basic`, the remaining single-failure basic plumbing test.

## Findings
- The failing subtest is `no diff after checkout and git update-index --refresh`.
- This points at index/worktree stat refresh or checkout metadata rather than object hashing.
- `update-index --refresh` verified content but only refreshed mtime/size/ctime, leaving dev/ino/uid/gid stale at zero.

## Work
- After content verification, refresh the complete stat tuple via `entry_from_metadata`.

## Verification
- `cargo build --release -p grit-cli`: pass with existing warnings.
- `./scripts/run-tests.sh t0000-basic.sh t0081-find-pack.sh`: both pass, `92/92` and `4/4`.
