# t3701-add-interactive — work log (session 2026-06-07 T1830)

Ticket 9b1d11. Started at 109/130 (per run-tests.sh).

## Important harness note (cost me time, documenting for next agent)
Running `tests/t3701-add-interactive.sh` DIRECTLY (not via run-tests.sh) inherits the
agent shell's `GIT_EDITOR=true`. `resolve_commit_launch_editor` gives `GIT_EDITOR` top
priority, so every `e` edit becomes a no-op (`true`) and the hunk-edit tests (19,21,24,28)
*appear* to fail. run-tests.sh strips it with `env -u GIT_EDITOR`. ALWAYS reproduce with
`env -u GIT_EDITOR -u GIT_DIR -u GIT_WORK_TREE EDITOR=: VISUAL=: LC_ALL=C LANG=C TZ=UTC ./t3701...`
or just trust run-tests.sh. Tests 19/21/24/28/83/103 are NOT real failures.

## Fixes this session

### "No newline at end of file" in add -p (test 38 "add first line works")
Two bugs, both around a worktree file whose final line has no trailing newline
(`firstline\nbaseline\ncontent\nlastline` w/ no `\n`):

1. **Staged blob got a spurious trailing newline.**
   `checkout::blend_line_diff_by_hunk_ranges` (shared by add -p / checkout -p staging)
   rebuilt content with `.lines()` + always `push('\n')`, so a no-final-newline file was
   staged WITH a newline → wrong blob hash. Fixed: track which side (source/worktree)
   produced the final emitted line and drop the trailing `\n` when that side had none.
   (grit/src/commands/checkout.rs, blend_line_diff_by_hunk_ranges)

2. **Hunk display omitted `\ No newline at end of file`.**
   `render_hunk_with_offsets` never emitted the marker. Added `old_no_nl`/`new_no_nl`
   params; each body line records whether it is the file's last line on its side, and the
   marker is printed right after it. Threaded the two no-nl flags through all 3 call sites.
   (grit/src/commands/add_patch.rs)

Result: 110/130 (test 38 passes; staged result + displayed hunks both match upstream).

## Remaining failing (20), with triage
- 43 adding an empty file — checkout/add -p empty-file add header.
- 55,113 edit/split interaction — cur_work mutation design flaw (per prior agents).
- 59,60 add -p color override / brackets — color infra edge.
- 76,77,78 submodules — large feature.
- 81,82,84,87 pathological-context edit / suppressBlankEmpty.
- 85 add--helper help text colorized.
- 92-96 -U/--inter-hunk-context across checkout/restore/commit/reset/stash -p.
  ROOT CAUSE CONFIRMED: those 5 commands use a SEPARATE hand-rolled patch loop
  (checkout.rs checkout_patch_inner uses `similar` `.unified_diff().context_radius(3)`
  HARDCODED; stash/reset/commit similar). add -p (test 91) already passes because it uses
  add_patch.rs natural_hunk_ranges with inter-hunk merge. Fixing 92-96 needs each of the
  5 sites to honor -U + merge adjacent hunks within 2*ctx+inter — 5 separate refactors.
- 124,130 >/< file nav with persistent per-file decisions — needs index-based file loop.
