# t6430 merge recursive

Claimed `t6430-merge-recursive.sh` from `t6-plan.md` at 11/36 passing.

Initial focus:

- Run the focused harness to identify the highest-impact remaining merge-recursive failures.
- Compare the local harness test with upstream `git/t/t6430-merge-recursive.sh`.
- Read the merge documentation and inspect existing merge-recursive implementation before editing.

Checkout/submodule blocker:

- Initial focused harness: `./scripts/run-tests.sh t6430-merge-recursive.sh --verbose` was 11/36.
- `setup 8` failed before merge-recursive assertions because `git checkout rename` from the
  `submod` branch aborted with `error: refusing to replace submodule at 'd' with directory content`.
- The failing guard was `refuse_populated_submodule_tree_replacement`, whose docs say it is
  rebase-only. `checkout_merged_index` already calls that guard explicitly for rebase, so the
  generic `checkout_index_to_worktree` path should not apply it to normal branch checkout.

Implementation:

- Removed the rebase-only replacement refusal from the generic checkout worktree writer.
- Left the explicit rebase guard in `checkout_merged_index` intact.

Validation:

- `cargo build --release -p grit-cli` completed with the existing warning backlog.
- Focus harness improved: `./scripts/run-tests.sh t6430-merge-recursive.sh --verbose` now reports
  28/36.
- Regression harness: `./scripts/run-tests.sh t6430-merge-recursive.sh t3426-rebase-submodule.sh t2013-checkout-submodule.sh t7112-reset-submodule.sh t6438-submodule-directory-file-conflicts.sh --verbose --timeout 180`
  reports `t6430` 28/36, `t3426` 11/29, `t2013` 62/74, `t7112` 78/82 with no failing tests, and
  `t6438` 56/56.
- `cargo fmt`, `cargo check -p grit-cli`, `cargo test -p grit-lib --lib`, and
  `cargo clippy --fix --allow-dirty` completed with the existing warning backlog.
- Restored unrelated clippy auto-fixes before commit.

Remaining:

- First remaining `t6430` failure is test 16, the result check after the D/F simple
  `merge-recursive "$c0" -- "$c1" "$c3"` case.
