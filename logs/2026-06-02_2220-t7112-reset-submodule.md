# t7112-reset-submodule

## 2026-06-02 22:20

- Claimed `t7112-reset-submodule.sh` after `t7400-submodule-basic.sh` reached 124/124.
- Fresh harness baseline was 34/82 passing after rerunning
  `./scripts/run-tests.sh t7112-reset-submodule.sh --verbose`; the prior plan count was stale.
- Relevant documentation read: `git-reset(1)` `--recurse-submodules`, `--keep`, `--merge`, and
  `--hard` behavior.
- Root cause for many early failures: fixture branch creation stopped at
  `replace_sub1_with_file` because checkout left `sub1` as a populated submodule containing only
  `.git`, so `git rm sub1` saw deleted tracked files and refused removal.
- Fixed checkout gitlink materialization to force-populate an existing same-OID submodule when the
  worktree has only `.git` left.
- Verification: `cargo fmt`; `cargo build --release -p grit-cli` passed with the existing warning
  backlog; `./scripts/run-tests.sh t7112-reset-submodule.sh --verbose` now reports 54/82.

## 2026-06-02 23:45

- Fixed `reset --keep --recurse-submodules` to let the two-way read-tree phase remove clean
  gitlinks, including uninitialized gitlink directories, when recursion is explicit.
- Routed recursive keep/merge reset gitlink removals through the reset submodule worktree cleanup
  path so removed submodule worktrees and module config are cleared consistently.
- Fixed `reset --hard --recurse-submodules` when replacing a submodule gitlink with a regular file:
  after removing the submodule worktree, reset now continues and writes the replacement blob.
- Fixed non-recursive reset gitlink materialization so a file at the future submodule path is
  replaced with an empty directory.
- Verification: `cargo fmt`; `cargo build --release -p grit-cli` passed with the existing warning
  backlog; targeted `--run=6`, `--run=28,37`, and `--run=43` checks passed; full
  `./scripts/run-tests.sh t7112-reset-submodule.sh --verbose` now reports 61/82.

## 2026-06-03 00:35

- Fixed non-recursive `reset --hard` to preserve checked-out submodule worktrees unless submodule
  recursion is explicit, and added an early atomic failure when hard reset would replace a
  populated gitlink with ordinary file or directory content.
- Fixed `reset --keep` pure gitlink removals to update the superproject while leaving the
  submodule worktree in place, without loosening gitlink-to-directory replacement conflicts.
- Fixed `reset --merge` safety for gitlink-only superproject updates: pure removals and gitlink
  OID changes now update the index without requiring the submodule worktree to match.
- Verification: `cargo fmt`; `cargo build --release -p grit-cli` passed with the existing warning
  backlog; targeted `--run=73-78`, `--run=45-50`, and `--run=55-68` checks showed the intended
  cases passing; full `./scripts/run-tests.sh t7112-reset-submodule.sh --verbose` now reports
  69/82.
