# t1 family completion log

## Scope

User requested that all `t1` family tests pass. Current work follows the dependency-group plan,
one file at a time, with harness runs updating `data/test-files.csv`.

## Initial CSV state

- `t1` total rows: 376.
- In scope: 368.
- Non-green in-scope rows: 134.
- Skipped rows to audit: 8.

## Group 0 — harness/catalog anomalies

### `t12280-log-shortlog-format.sh`

- Starting state from CSV: status `timeout`, tests `0`, pass `0`, fail `0`.
- Initial hypothesis: this synthetic fixture may be timing out because it uses direct `grit`
  invocations and starts by changing to the `tests/` directory; need determine whether the issue is
  a real `grit log` hang or a harness fixture issue before modifying Rust.
- Reproduced directly with the harness environment: setup passed, but it left the shell inside
  `repo`, so later tests tried `(cd repo ...)` from `repo/` and failed. One test used `|| exit 1`,
  which aborted the file before `test_done`, explaining the CSV timeout / zero-count result.
- Fixed by wrapping only the setup body in a subshell so the top-level cwd returns to the trash
  root before subsequent tests.
- Verification: `./scripts/run-tests.sh --timeout 60 --verbose t12280-log-shortlog-format.sh`
  passes 36/36 and updates the CSV/dashboard row to green.

### `t1509-root-work-tree.sh`

- Starting state from CSV: status `ok`, tests `0`, pass `0`, fail `0`, `fully_passing=false`.
- Verification/audit: running the file directly under the harness environment prints
  `1..0 # SKIP Test requiring writable / skipped. Read this test if you want to run it` followed by
  a zero-test summary.
- The test intentionally requires a writable filesystem root, `IKNOWWHATIAMDOING=YES`, and a
  non-root/safe environment. It would create `/foo`, `/me`, and root-level Git files, so it is not
  safe for the normal harness. Marked the row `in_scope=skip` and regenerated dashboards so it is
  not counted as a misleading non-green in-scope `t1` row.

## Group 1 — read-tree/sparse/index/object foundations

### `t1001-read-tree-m-2way.sh`

- Starting state from CSV: 25/29, failing 4.
- Verification: `./scripts/run-tests.sh --timeout 120 --verbose t1001-read-tree-m-2way.sh`
  now passes 29/29 with the current codebase; no Rust change was needed for this file.

### `t1002-read-tree-m-u-2way.sh`

- Starting state after harness refresh: 19/22, failing 3.
- Failures were all `check_cache_at <path> clean` assertions after `read-tree -m -u` wrote the
  expected worktree bytes. The index entries had stale stat data, so a following `git diff-files`
  reported paths as dirty even when the content matched the indexed blob.
- Fixed `read-tree -u` to refresh cached index stat data for content-verified stage-0 entries after
  checkout and before writing the index, matching the same guarded stat refresh used by
  `status`/`reset`/`stash`.
- Verification:
  - `cargo fmt`
  - `cargo build --release -p grit-cli` (passes with existing warning backlog)
  - `./scripts/run-tests.sh --timeout 120 --verbose t1002-read-tree-m-u-2way.sh` passes 22/22
  - `./scripts/run-tests.sh --timeout 120 --verbose t1001-read-tree-m-2way.sh
    t1002-read-tree-m-u-2way.sh` passes 29/29 and 22/22.
