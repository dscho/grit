# t7400-submodule-basic — submodule porcelain fixes (Phase 7b)

Branch: `wf/p7b/t7400` (worktree `/Users/schacon/grit-worktrees/wf-p7b-t7400`, off `main` @ `ce7e755cc`)
Result: **63/124 → 111/124** (target was 100+).

## Approach

Aligned grit's `submodule` porcelain with git's `submodule--helper`: the
submodule work set is derived from **index gitlink entries**
(`module_list_compute`), not just `.gitmodules`, and pathspecs that match no
gitlink are an error.

## Subtests fixed (48)

- Pathspec validation (status/init/update/sync): 11, 12, 14, 40, 48–51, 57
- `submodule add` path handling: 8, 13, 23, 26, 30, 31, 33, 34, 36, 37, 111
- `submodule add` checks/messages: 24, 25, 27, 28 (and 13/26 fatal wording)
- `submodule deinit` full port: 98, 100, 101, 102, 103, 104, 105, 106, 107, 108
- `submodule.active` wiring: 113, 114, 115, 116, 117, 121
- relative scp-style URL: 83
- `clone --recurse-submodules=<pathspec>`: 118, 119, 120, 121
- update refuses occupied path / status unmapped gitlink: 40, 52, 53

## Changes

### grit/src/commands/submodule.rs
- `index_gitlink_paths`, `normalize_submodule_path_arg`, `lexically_normalize`,
  `validate_submodule_pathspecs`, `pathspec_select_gitlinks`,
  `config_submodule_url`, `submodule_path_is_active` — new helpers.
- `run_status`: validate pathspecs; die "no submodule mapping found in
  .gitmodules" for selected index gitlinks lacking a `.gitmodules` entry.
- `init_in_repo`: pathspec selection, active-only default when `submodule.active`
  set and no args, set `submodule.<name>.active=true`, die "No url found".
- `run_add`: prefix sm_path from subdir + reject relative repo URL, normalize
  `./ /./ /../ //` and trailing slashes (`git_path::normalize_path_copy`),
  `validate_submodule_path`, die_on_index_match (pathspec prefix + fatal:),
  die_on_repo_without_commits, `add --dry-run` probe relay (SilentNonZeroExit),
  name-already-used check, `-b <branch>` HEAD attach, `--force` staging,
  `--progress/--depth/--reference/--dissociate` clone passthrough.
- `run_deinit`: faithful `module_deinit` port (index gitlinks, `--all`/path
  exclusivity, dirty check via `rm -qn` + `status --porcelain`, Cleared
  directory / unregistered messages, unset core.worktree, recreate empty dir).
- `parse_gitmodules_with_repo`: keep path-only sections (url may be empty).
- `url_is_local_not_ssh`: faithful port (scp-style `user@host:repo` is SSH).
- `AddArgs`: added `--progress/--depth/--reference/--dissociate`.

### grit/src/commands/_submodule_run_update_inner.rs.inc
- Validate pathspecs; "not initialized" for unmapped index gitlinks with args.
- Gate on `is_submodule_active` (not just local url); die "cannot clone
  submodule '<name>' without a URL" for active url-less subs.
- Refuse to clone over an occupied path (file / non-empty dir).

### grit/src/commands/clone.rs
- `--recurse-submodules` now takes an optional repeatable pathspec
  (`recurse_submodules_specs`, `recurse_submodules()` / `_pathspecs()`); records
  `submodule.active` and clones only matching submodules.

### grit/src/commands/add.rs
- Early index-lock check (even with `--dry-run`) so `git submodule add` relays
  git's `fatal: ... index.lock` (t7400.28).

## Guard counts (after, vs CSV baseline)
- t7400: 111/124 (was 63)
- t7403: 18/18 (CSV 16) — no drop
- t7406: 40/70 (CSV 34) — improved, no drop
- t7506: 28/40 (CSV 28) — stable
- t6437: 20/22 (CSV 20) — stable
- t4059: 7/8 (CSV 7) — stable
- t2013: 7/74, t1013: 2/68, t5601: 66/49, t5606: 20/1, t5617: 2/7 — all match
  CSV baseline (no regression); t5614 improved 2/9 → 9/9.
- `cargo test -p grit-lib --lib`: 229 passed.

## Out of v1 scope (remaining 13)
- 123 `init.templateDir` — needs post-checkout **hook execution** during add.
- 94, 96 — depend on `git rm` of a **gitlink** (rm subsystem; setup fails).
- 64, 65, 69, 70, 71 — gitlink interactions in **checkout / apply / commit /
  diff / ls-files** (other subsystems; pass in isolation but cascade here).
- 66, 67 — `update --init` edge cases (all-zero recorded OID; subdir rev-parse).
- 29 — `add -b` then `submodule init` in a multi-submodule `addtest` (passes
  standalone; cascades from prior addtest state).
- 35 — backslash-in-path submodule add (non-Windows literal filename edge).
- 91 — `add ../bar/a/b/c ./a/b/c` deep relative add + url resolution.
