# t7418 + t7505 rebase prepare-commit-msg (2026-06-02)

## t7418-submodule-sparse-gitmodules — 9/9 pass

- `submodule summary --for-status` used abbreviated OIDs in nested `rev-list`; grit could not resolve them → count `-1` and `:` output.
- Fix: use full hex for `rev-list`/`log`; call `submodule_fetch_gitlink_if_missing` before counting when gitlink OIDs are missing from nested ODB.

## t7505-prepare-commit-msg-hook — 22/23

### Landed

- `prepare-commit-msg` hook args: optional third arg (`commit`/`HEAD` for reword amend).
- Two-step reword: pick with `message` + `GIT_EDITOR=:`, then amend editor with `commit`/`HEAD`.
- `GIT_EDITOR=:` on sequencer pick path via `commit_message_after_prepare_hook`.
- `rebase-merge/done` appended before each interactive pick (match Git `save_todo` timing).
- Conflict continue: hook arg `merge` when `MERGE_MSG` exists.
- Rebase cherry-pick merge: `MergeFavor::Theirs`.
- Index: do not write in-memory `FLAG_EXT_FSMONITOR_VALID` (0x1000) to disk (Git dies on `0x10000000`).

### Remaining (test 16)

Interactive rebase -i with hook: subshell still fails; `actual` sometimes shows `'prepare-commit-msg' hook failed` instead of 18 reflog subjects. Needs further hook/done/rebase -i debugging.
