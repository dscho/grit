## t6300-for-each-ref

Goal: make `tests/t6300-for-each-ref.sh` fully pass as the first t6 ref-format dependency group item.

Initial CSV state: 429 tests, 347 passed, 82 failing.

Worktree: `/private/tmp/grit-t6-family` on branch `wf/t6-family`.

Notes:
- Current `target/release/grit` is absent in this worktree, so the first test run needs a release build here per the user's worktree-only instruction.
- Built `grit-cli` in the worktree. Existing warnings were present.
- `./scripts/run-tests.sh t6300-for-each-ref.sh --verbose` refreshed the baseline at 347/429.
- Direct verbose run without `TZ=UTC` first failed at `default-local`; that is an environment artifact because the simplified harness does not set upstream's UTC test timezone.
- Direct verbose run with `TZ=UTC` first failed at `%(upstream:track)`, which printed `[differs]` instead of `[ahead 1]`.
- Patched `for_each_ref` to use `count_symmetric_ahead_behind` for `upstream:track`, `upstream:trackshort`, `push:track`, and `push:trackshort`, including `nobracket`.
- Rebuilt and reran with `TZ=UTC`; tracking tests now pass through `push:trackshort`.
- Next failure: `%(color:green)` was unsupported. Patched minimal color atom support with `--color` / `--no-color` parsing, empty expansion when color is disabled, ANSI color output when enabled, and automatic reset at end of formatted line.
- Continued through the remaining ref-filter coverage:
  - Added describe atom support and `describe --exclude`.
  - Added raw byte output/quoting validation, multiple sort keys, contents/raw size sorting, custom date sorting, `--no-sort`, and `--omit-empty`.
  - Added trailer atoms and conditional formatting support.
  - Added symref, remote name/ref, push refspec, ambiguous short-ref, and strict atom argument handling.
  - Added `tag --cleanup=verbatim` so CRLF signature fixtures can be created.
  - Fixed recursive peeling for dereferenced atoms on nested annotated tags.
  - Added `%(is-base:<commit>)` winner selection and non-commit diagnostics.
  - Added `%(signature:...)` fields using the existing signing verifier for signed commits/tags.
- Verification:
  - `cargo check -p grit-cli` passed with the existing warning backlog.
  - `cargo build --release -p grit-cli` passed with the existing warning backlog.
  - `TZ=UTC sh ./t6300-for-each-ref.sh -i` passed all 429 tests.
  - `TZ=UTC ./scripts/run-tests.sh t6300-for-each-ref.sh` passed 429/429 and refreshed `data/test-files.csv` plus dashboards.
- Post-rebase verification also ran `TZ=UTC ./scripts/run-tests.sh t6301-for-each-ref-errors.sh
  --verbose`, which passed 6/6 after ignored broken/zero loose refs were made to remove any
  preloaded refs-list entry.
