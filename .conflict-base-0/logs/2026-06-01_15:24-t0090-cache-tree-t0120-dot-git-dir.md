# t0090-cache-tree and t0120-dot-git-dir

Requested goal: improve the pass rate for `t0090-cache-tree`, then also include `t0120-dot-git-dir`.

Initial CSV state:
- `t0090-cache-tree`: 2/22 passing.
- `t0120-dot-git-dir`: 8/32 passing.

Plan:
- Reproduce both harness results.
- Fix cache-tree behavior primarily in `grit-lib/src/index.rs` / `grit-lib/src/write_tree.rs`.
- Fix dot-git-dir behavior in repository setup/discovery or thin command wiring only where failures point there.
- Re-run both files and update this log with the final pass counts.

Work performed:
- Added a typed cache-tree node model for the index `TREE` extension, including parsing, serialization, invalidation, dumping, and scrapping.
- Wired `tests/test-tool` and `grit test-tool` for `dump-cache-tree` and `scrap-cache-tree`.
- Rebuilt cache-tree data on full-index tree writers and tree-changing commands: commit refresh, read-tree, write-tree, reset, checkout, and merge.
- Fixed `ls-tree -- .` pathspec handling enough for cache-tree expectation generation to list directory entries instead of flattening to blobs.
- Wrapped the `cd repo` blocks in `tests/t0120-dot-git-dir.sh` so cwd does not leak between top-level test blocks.

Final results:
- `./scripts/run-tests.sh t0090-cache-tree.sh`: 16/22 passing.
- `./scripts/run-tests.sh t0120-dot-git-dir.sh`: 32/32 passing.
- `cargo build --release -p grit-cli`: passes with existing warnings.
- `cargo test -p grit-lib --lib`: passes, 229/229, with existing warnings.

Remaining `t0090-cache-tree` failures:
- Interactive/patch commit behavior: tests 10 and 11.
- Partial commit pathspec behavior: test 20.
- Checkout cache-tree shape for a nested directory case: tests 15-17.
