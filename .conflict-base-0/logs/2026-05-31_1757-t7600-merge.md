# t7600-merge — porcelain merge (non-interactive)

Branch: `wf/p6/t7600-merge`
Result: **50/83 → 81/83** (official harness), no regressions in guard files.

## What changed (all in `grit/src/commands/`, no `tests/` edits)

### merge.rs
- **Reflog on HEAD updates** (`merge_update_head_with_reflog` + `reflog_identity` +
  `merge_reflog_action`): FF → `merge <args>: Fast-forward`; unborn FF → `initial pull`;
  real/octopus → `merge <args>: Merge made by the '<strategy>' strategy.`; `--continue` →
  `commit (merge): <subject>`; in-index trivial → `In-index merge`. Fixes #5, #11, #47-reflog.
- **Diffstat base bug**: real/octopus merge diffstat now diffs HEAD vs the *resulting* merge
  commit tree (`commit_oid`), not the side branch (`merge_oid`). Fixes #33, #34.
- **`-n` parsing**: `git merge -n` is `--no-stat` (removed the duplicate `short='n'` on
  `--no-verify`, matching git). Fixes the #35 regression that the diffstat fix exposed.
- **Autostash = git's `MERGE_AUTOSTASH` stash-ref model** (replaced the snapshot model):
  create before FF/real/octopus merges; apply on a clean committed merge; keep for
  conflict/squash/`--no-commit` (printing "When finished, apply stashed changes with
  `git stash pop`"); `merge --abort` applies it; `merge --quit` saves it. Reads
  `merge.autoStash`. Fixes #15, #54-66.
- **SQUASH_MSG / merge log format**: no blank between commits (match grit `log` medium),
  no extra trailing blank, unpadded day; squash-conflict adds a blank before `# Conflicts:`.
  Fixes #24/25/26/28/32.
- **Conflict hint**: conflicted merges append git's `# Conflicts:` block, and under
  `commit.cleanup=scissors` the cut-line block (`append_conflicts_hint`). Fixes #16, #17.
- **Strict merge-arg resolution**: a bare merge arg no longer DWIMs to
  `refs/remotes/<remote>/<name>`; an unresolvable name dies `… - not something we can merge`
  with `Did you mean …` suggestions from `refs/remotes/*`. Fixes #82, #83.
- **`(early part)` merge message** for `<branch>~<n>` / `<branch>^` specs. Fixes #67.
- **Really trivial in-index merge** (`-s resolve`/`octopus`, single-head, non-FF): prints
  `Trying really trivial in-index merge…`/`Wonderful.`, commits as a trivial 3-way, reflog
  `In-index merge`. Fixes #52.

### stash.rs
- `create_autostash_ref` / `apply_autostash_ref` / `save_autostash_ref` (pseudo-ref under
  `<git_dir>/MERGE_AUTOSTASH`), paralleling the rebase autostash helpers.
- `stash show -p` tracked patch now uses 3 lines of context (was 0), matching git
  (also improved t3905 29→31).

### commit.rs / reset.rs
- `git commit` applies `MERGE_AUTOSTASH` when concluding an autostash merge; `reset --hard`
  saves it (`remove_branch_state`). Plus `MERGE_RR`/`AUTO_MERGE` cleanup. Fixes #61-65.

### show.rs
- Unpadded day in the default ("medium") date format (git `"%.3s %d "`).
- `--pretty=format:` does not terminate an empty *last* entry (`git show -s --pretty=format:%b`
  on a body-less commit prints nothing); `tformat:` still appends a terminator. Fixes #47.

## Remaining 2 failures (not landed)
- **#70 merge --no-ff --edit**: BLOCKED by the environment — the user's shell exports
  `GIT_EDITOR=true`, which leaks into the harness (run-tests.sh does not unset GIT_EDITOR).
  Real git honors GIT_EDITOR first too, so it also fails here. grit's editor handling is
  already correct; not a grit bug.
- **#71 merge annotated tag w/o tracking**: requires `git pull` to record annotated tags in
  FETCH_HEAD as `tag '<name>'` (fetch-side) and merge to force `--no-ff` for a non-follow-along
  tag (`merging_a_throwaway_tag`). Cross-cutting fetch+pull+merge change; deferred as
  out-of-scope / risky per the diagnosis.

## Regression guards (all unchanged)
t7110-reset-merge 21/21, t5813-proto-disable-ssh 81/81, t5563-simple-http-auth 17/17,
t5547-push-quarantine 6/6. Also verified: t3903-stash 105 (+t3905 29→31), t7102-reset 38,
t4205-log-pretty 48, t6402-merge-rename 24, t7601 63, t7604 8, t6437 20 — no regressions.
(Note: t7611-merge-abort CSV said 19 but the pre-existing `merge --abort` worktree-restore
already failed #12/#13 at the base commit; the merge_abort worktree code is byte-identical to
base — the CSV count was stale, not a regression.)

cargo fmt clean; cargo test -p grit-lib --lib = 228 passed.
