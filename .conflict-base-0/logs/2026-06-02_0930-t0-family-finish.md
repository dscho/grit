# t0 family finish

## Scope

Finish the current in-scope t0 family. Initial refreshed CSV showed six non-green rows:
`t0110`, `t0600`, `t0610`, `t0611`, `t0613`, and `t0614`.

## Work completed

- Refreshed stale green rows:
  - `t0110-environment.sh` now records 31/31.
  - `t0600-reffiles-backend.sh` now records 33/33.
- Reftable transactions:
  - Added single-table reftable transaction writing for implicit `update-ref --stdin` batches.
  - Fixed the `t0613` many-refs block layout case.
  - Kept branch ref+reflog creation compact under locked-table scenarios so `t0610` auto-compaction
    failure handling lands at the expected stack size.
  - Added a default reftable lock retry window so transient stack locks do not surface as spurious
    failures while still preserving lock-error behavior.
- Reftable verification:
  - Allowed root `HEAD` during reftable verify.
  - Verified reftable table names, broken worktree stacks, and invalid symref referents.
  - `t0614-reftable-fsck.sh` now passes 7/7.
- Reftable HTTP:
  - Routed smart HTTP upload-pack requests for reftable repositories through Grit's upload-pack path
    in the lightweight test HTTP server instead of system Git's advertise-refs path.
  - `t0611-reftable-httpd.sh` now passes 1/1.
- Final t0 sweep follow-ups:
  - Normalized local UTC `%Z` date formatting to `UTC` (`t0006-date.sh`).
  - Added smudged racy index handling for `update-index`/`diff-files` (`t0010-racy-git.sh`) without
    regressing CRLF-clean stat matches.
  - Wrapped unicode cwd-leaking bodies in `t0050-filesystem.sh` per the documented harness cwd trap.

## Verification

- `cargo build --release -p grit-cli --bin grit --bin test-httpd` passed with existing warnings.
- `cargo check -p grit-cli` passed with existing warnings.
- `cargo clippy --fix --allow-dirty --allow-staged` completed; unrelated auto-fixes in
  `grit-lib/src/config.rs` and `grit-lib/src/filter_process.rs` were reverted.
- `cargo test -p grit-lib --lib` passed.
- `./scripts/run-tests.sh t0 --verbose` passed all 70 in-scope t0 files.

