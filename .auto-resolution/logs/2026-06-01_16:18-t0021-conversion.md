# t0021-conversion

## Claim
- Claimed `t0021-conversion` as the next conversion/filter lane dependency after `t0020` and `t0023`.

## Findings
- Current harness result is 27/42.
- The first failure is not process-filter-specific: `git diff --raw --exit-code :test :test.i` reports a modification even when both blob specs resolve to the same blob ID and mode.
- The remaining failures are concentrated in long-running `filter.<driver>.process` checkout/smudge/delay behavior.
- The next process-filter failure was blocked before filtering: `git checkout --quiet --no-progress .` treated `--no-progress` as a revision and exited before restoring deleted paths.

## Work
- Started with the blob-vs-blob diff correctness issue: suppress diff entries when both resolved blob sides have identical object IDs and modes.
- Added `checkout --no-progress` parsing so path checkouts do not pass it through as a tree-ish/path token.

## Verification
- `cargo build --release -p grit-cli` passes with the existing warning backlog.
- `./scripts/run-tests.sh t0021-conversion.sh t0023-crlf-am.sh t0020-crlf.sh` improves `t0021-conversion` to 28/42 and keeps `t0023` 2/2 and `t0020` 36/36.
