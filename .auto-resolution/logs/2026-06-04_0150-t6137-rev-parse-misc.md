# t6137 rev-parse misc

Claimed `t6137-rev-parse-misc.sh` from `t6-plan.md` at 23/34 passing.

Initial focus:

- Run the current harness/direct test to identify the first failing rev-parse behavior.
- Read the upstream test and `git rev-parse` documentation before changing parsing behavior.
- Search existing rev-parse code paths and keep fixes in `grit-lib` unless the issue is CLI-only
  argument routing or output formatting.

Findings:

- Direct runs with the default local environment passed 34/34.
- The official harness still reported 23/34 because `scripts/run-tests.sh` exports
  `GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME=main`, while this synthetic test hard-coded `master`
  branch names and refs.
- There is no upstream `git/t/t6137-rev-parse-misc.sh`; the neighboring upstream t6137 file is
  the pathspec wildcard test. This is a local synthetic fixture, matching other plan entries that
  explicitly request `master` when their assertions are written around that branch.

Changes:

- Exported `GIT_TEST_DEFAULT_INITIAL_BRANCH_NAME=master` before sourcing `test-lib.sh`.

Validation:

- `cargo build --release -p grit-cli` completed with the existing warning backlog.
- `./scripts/run-tests.sh t6137-rev-parse-misc.sh` passes 34/34 and refreshes
  `data/test-files.csv` plus dashboards.
